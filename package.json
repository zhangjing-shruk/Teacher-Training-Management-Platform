{
  "name": "ai-teacher-training-platform",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev": "cd frontend && npm run dev",
    "install-deps": "cd frontend && npm install"
  }
}